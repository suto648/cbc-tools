# 自分のツールを CbC につなぐ

**この1枚を Claude に渡せば、つなぐ作業は代わりにやってもらえます。**
「このファイルを読んで、`<自分のツールのフォルダ>` を CbC に登録して」と頼んでください。

---

## これは何のための道具か

Claude と一緒に小さな常駐ツールを作っていくと、こうなります。

- どこのフォルダに作ったか忘れる
- いま動いているのか止まっているのか分からない
- 起動するたびにエクスプローラーを開いて、`.bat` を探して、ダブルクリックする
- PC を再起動したあと、何を上げ直すべきだったか思い出せない

CbC は、**それらを1か所にまとめて、状態を1枚の窓で見えるようにする**ためのものです。
ツールそのものは作りません。**登録簿と起動係**です。中身は持ち主のものです。

---

## つなぐのに必要なこと（最小）

### 1. 登録簿に1件足す

`hub\registry.json` の `tools` に追記します。最小はこれだけです。

```json
{
  "id": "my-tool",
  "name": "私のツール",
  "desc": "窓に出る一行の説明",
  "mode": "daemon",
  "implemented": true,
  "dir": "{TOOLS}\\my-tool",
  "launch": { "exe": "node", "args": ["{TOOLS}\\my-tool\\main.js"] },
  "probe": { "type": "heartbeat", "maxAgeSec": 8 },
  "autostart": { "type": "hub", "enabled": false },
  "log": "{LOGS}\\my-tool.log"
}
```

**パスは目印で書いてください。** 絶対パスを書くと、そのPCでしか動かなくなります。

| 目印 | 指す場所 |
|---|---|
| `{CBC}` | CbC Tools を置いた場所 |
| `{TOOLS}` | `{CBC}\tools` |
| `{LOGS}` | `{CBC}\logs` |
| `{HOME}` | あなたのユーザーフォルダ（`C:\Users\<名前>`） |
| `%VAR%` | 環境変数（見つからなければそのまま残ります） |

### 2. 生きていることを知らせる

2秒ごとに、この1本を投げるだけです。

```
POST http://127.0.0.1:47821/api/state/my-tool
Content-Type: application/json

{ "level": "ok", "detail": "人が読む一言", "pid": 1234 }
```

- `level` は `ok` / `warn` / `error` の3つ。窓の色になります。
- `detail` はそのまま窓に出ます。数字を入れると役に立ちます（「3件処理」「接続あり」など）。
- **8秒届かないと「停止中」になります。**
- CbC が動いていないときは、送信の失敗を黙って捨ててください。
  ここで落ちると、ハブを止めただけで自分のツールまで巻き添えになります。

見本: `tools\sample-daemon\main.js`（Node・依存なし・50行）
Python なら `tools\_shared\cbc.py` に同じものがあります。

---

## `mode` の選び方

| mode | どういうときに | CbC がすること |
|---|---|---|
| `daemon` | 自分で起動・停止したい常駐ツール | 起動・停止・自動起動を持つ |
| `scheduled` | Windows のタスクが持っている | 有効／無効の切り替えだけ |
| `ondemand` | 押したときだけ動いて終わる | 起動だけ |
| `external` | 別の何かが起動している | **見ているだけ。止めない** |

迷ったら `daemon` です。
**`external` は「勝手に止められると困るもの」に使ってください。**
別のセッションで開発中のアプリなどが該当します。

## `probe`（生きているかの見方）の選び方

| type | 見るもの | 向いているとき |
|---|---|---|
| `heartbeat` | ツールからの報告 | **いちばん正確。書き換えられるなら必ずこれ** |
| `process` | 実行ファイル名 | 中身を書き換えられないとき |
| `task` | Windows タスクの状態 | `scheduled` のとき |
| `http` | URL が応答するか | サーバ型のツール |

---

## 気をつけること（実際に踏んだもの）

**止めたときに環境を戻す。**
マイクをミュートする、既定のデバイスを変える、といったツールは、
終了時に必ず元へ戻してください。`registry.json` の `restore` に一言書き、
`stopOrder` を小さくすると先に止まります。

**黙って「正常」に見えないようにする。**
状態が取れなかったときは、`ok` ではなく `warn` を返してください。
「取りに行っていない」「取れなかった」「取れた上で異常」は**3つとも別の状態**です。
混ぜると、知らないまま知っているふりになります。
（この道具は、壊れているのに成功と報告するタスクに11日気づけなかったことから作られました）

**ログは1本のファイルに追記する。**
`log` に書いたファイルは窓から開けます。ローテーションは要りません。

**昇格（管理者権限）が要るなら `elevated: true`。**
CbC 自身は昇格しません。タスク経由で起動します。

---

## 確かめ方

1. `CbC起動.bat` を実行する
2. 窓に自分のツールが出ることを確認する（まだ灰色）
3. 「起動」を押す → 数秒で緑になれば、つながっています
4. 緑にならないときは、窓からログを開く。
   それでも分からなければ `logs\hub.log` を見る

---

## Claude に頼むときの言い方

> `CONNECT.md` を読んで、`C:\...\my-tool` を CbC に登録してください。
> heartbeat を送る処理も main.js に足してください。

読ませるファイルは `CONNECT.md` と `hub\registry.sample.json` の2つで足ります。
